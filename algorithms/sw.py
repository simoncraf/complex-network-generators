
def generate_sw_network(N: int, k: int, p: float):
    print("Not implemented yet")
    #graph.nodes = [Node(i) for i in range(N)]
    #for i in range(N):
    #    for j in range(1, k // 2 + 1):
    #        graph.add_edge(graph.nodes[i], graph.nodes[(i + j) % N])
    #        graph.add_edge(graph.nodes[i], graph.nodes[(i - j) % N])
    #for i in range(N):
    #    for j in range(i + 1, N):
    #        if random.random() < p:
    #            graph.add_edge(graph.nodes[i], graph.nodes[j]